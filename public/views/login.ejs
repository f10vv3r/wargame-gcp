<!DOCTYPE html>
<html>

<head>
    <title>f10w3r_wargame</title>
    <meta charset="utf-8">
    <style id="theme-color">
        body {
            background-color: black;
            color: white;
            margin: 0;
            font-family: Arial, sans-serif;
        }
        h1 {
            text-align: center;
            color: white;
            margin-top: 50px;
            font-size: 32px;
        }
        nav {
            display: flex;
            justify-content: space-between;
            padding: 10px 50px;
            background-color: #222;
            position: relative;
        }
        .dropdown-content {
            display: none;
            flex-direction: column;
            background-color: #222;
            opacity: 0.8;
            width: 100%;
            text-align: center;
            position: absolute;
            left: 50%;
            transform: translate(-50%, 10px);
            z-index: 1;
        }
        .dropdown-content a {
            color: white;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
            width: 100%;
        }
        .dropdown-content a:hover {
            background-color: #3a3a3a;
        }
        .dropdown.active .dropdown-content {
            display: flex;
        }
        .dropdown button {
            padding: 10px 40px;
            background-color: #222;
            color: white;
            border: none;
            cursor: pointer;
        }
        .brand {
            color: white;
            text-decoration: none;
            font-size: 24px;
        }
        .form-container {
            display: flex;
            justify-content: center; /* 가로 방향 중앙 정렬 */
            align-items: center;
            margin-top: 80px; /* 세로 위치를 위쪽으로 유지 */
            width: 100%;
        }


        /* 배경 사각형 */
        .form-background {
            background: #333; /* 사각형 배경 색상 */
            border-radius: 10px; /* 둥근 모서리 */
            padding: 30px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); /* 그림자 효과 */
            
        }

        .form-container input {
            margin: 15px 0;
            padding: 15px 15px; /* 상하 패딩 15px, 좌우 패딩 15px */
            font-size: 18px;
            width: 300px;
            border-radius: 5px;
            border: 1px solid #ccc;
            background-color: #444;
            color: white;
            box-sizing: border-box; /* 크기 조정 포함 */
        }

        .form-container button {
            margin: 15px 0;
            padding: 15px 15px; /* 상하 패딩 15px, 좌우 패딩 15px으로 input과 동일 */
            background-color: #444;
            color: white;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 300px;
            box-sizing: border-box;
        }

        .form-container button:hover {
            background-color: #555;
        }

        .form-container a {
            color: #bbb;
            text-decoration: none;
            font-size: 16px;
        }

        .form-container a:hover {
            color: white;
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const dropdownButton = document.querySelector('.dropdown button');
            const dropdownMenu = document.querySelector('.dropdown');

            dropdownButton.addEventListener('click', function() {
                dropdownMenu.classList.toggle('active');
            });
        });
    </script>
</head>

<body>
    <!-- 네비게이션 바 -->
    <nav>
        <div>
            <a href="/" class="brand">f10w3r_wargame</a>
        </div>
        <div class="dropdown">
            <button type="button">
                Menu ▿
            </button>
            <div class="dropdown-content">
                <a href="/user">Users</a>
                <a href="/wargame">Wargame</a>
                <a href="/wargame/upload">Upload</a><br>
                <a href="/login">Login</a>
                <a href="/logout">Logout</a>
                <a href="/signup">Signup</a>
            </div>
        </div>
    </nav>
    <br><br>

    <div class="form-container">
        <div class="form-background">
            <h1>Login</h1>
            <form method="post">
                <input type="text" name="id" placeholder="Enter your ID" required><br>
                <input type="password" name="pw" placeholder="Enter your password" required><br>
                <button type="submit">Login</button>
            </form>
            <br>
            <div style="text-align: center;">
                <a href="/forgot">Forgot your password? Reset it here.</a>
            </div>
            
        </div>
    </div>

</body>

</html>
